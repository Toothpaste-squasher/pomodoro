import { createContext } from "react";

export const timerTimeContext = createContext(null);
export const timerCycleContext = createContext(null);
export const timerDispatchContext = createContext(null);